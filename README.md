# Pak Package Repository

The official package repository for **Pak**.

> **Status:**  Work in progress

## Package Structure

Each package is stored in its own directory:

```text
packagename/
├── package.yml
└── package.pak
```

Scaffold a new one interactively with `./scripts/new-package.sh` (just bash, nothing to install).
