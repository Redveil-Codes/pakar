CC=gcc
SRC=https://github.com/flameshot-org/flameshot
DEPENDENCIES=(cmake gcc make)
BUILDDIR=/tmp/pak/imports/flameshot
BIN=./build/src/flameshot
LIB=0
INSTALL_BIN=y
INSTALL_LIB=n
BUILD_SCRIPT=(
    mkdir -p build
    cd build
    cmake ..
    make -j16
)
