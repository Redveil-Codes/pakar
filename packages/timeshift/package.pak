CC=gcc
SRC=https://github.com/linuxmint/timeshift
DEPENDENCIES=(cmake gcc make)
BUILDDIR=/tmp/pak/imports/timeshift
BIN=./build/src/timeshift
LIB=0
INSTALL_BIN=y
INSTALL_LIB=n
BUILD_SCRIPT=(
    mkdir -p build
    cd build
    cmake ..
    make -j16
)
