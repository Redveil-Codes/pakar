CC=
SRC=https://github.com/llvm/llvm-project
DEPENDENCIES=(cmake ninja)
BUILDDIR=/tmp/pak/imports/lldb
BIN=./build/bin/lldb
LIB=0
INSTALL_BIN=y
INSTALL_LIB=n
BUILD_SCRIPT=(
    mkdir -p build
    cd build
    cmake -G Ninja ../llvm -DLLVM_ENABLE_PROJECTS=lldb
    ninja lldb
)
