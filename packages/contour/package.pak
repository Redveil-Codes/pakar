CC=g++
SRC=https://github.com/contour-terminal/contour
DEPENDENCIES=(cmake gcc make)
BUILDDIR=/tmp/pak/imports/contour
BIN=./build/src/contour/contour
LIB=0
INSTALL_BIN=y
INSTALL_LIB=n
BUILD_SCRIPT=(
    mkdir -p build
    cd build
    cmake ..
    make -j16
)
