CC=g++
SRC=https://github.com/cryfs/cryfs
DEPENDENCIES=(cmake gcc make)
BUILDDIR=/tmp/pak/imports/cryfs
BIN=./build/src/cryfs-cli/cryfs
LIB=0
INSTALL_BIN=y
INSTALL_LIB=n
BUILD_SCRIPT=(
    mkdir -p build
    cd build
    cmake ..
    make -j16
)
