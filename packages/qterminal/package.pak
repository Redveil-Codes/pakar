CC=g++
SRC=https://github.com/lxqt/qterminal
DEPENDENCIES=(cmake gcc make)
BUILDDIR=/tmp/pak/imports/qterminal
BIN=./build/qterminal/qterminal
LIB=0
INSTALL_BIN=y
INSTALL_LIB=n
BUILD_SCRIPT=(
    mkdir -p build
    cd build
    cmake ..
    make -j16
)
