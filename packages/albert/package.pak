CC=g++
SRC=https://github.com/albertlauncher/albert
DEPENDENCIES=(cmake gcc make)
BUILDDIR=/tmp/pak/imports/albert
BIN=./build/albert
LIB=0
INSTALL_BIN=y
INSTALL_LIB=n
BUILD_SCRIPT=(
    mkdir -p build
    cd build
    cmake ..
    make -j16
)
