CC=g++
SRC=https://github.com/KDE/konsole
DEPENDENCIES=(cmake gcc make)
BUILDDIR=/tmp/pak/imports/konsole
BIN=./build/bin/konsole
LIB=0
INSTALL_BIN=y
INSTALL_LIB=n
BUILD_SCRIPT=(
    mkdir -p build
    cd build
    cmake ..
    make -j16
)
