CC=cc
SRC=https://github.com/libfuse/sshfs
DEPENDENCIES=(gcc meson ninja)
BUILDDIR=/tmp/pak/imports/sshfs
BIN=./build/sshfs
LIB=0
INSTALL_BIN=y
INSTALL_LIB=n
BUILD_SCRIPT=(
    meson setup build
    ninja -C build
)
