CC=gcc
SRC=https://github.com/curl/curl
DEPENDENCIES=(autoconf automake gcc make)
BUILDDIR=/tmp/pak/imports/curl
BIN=./src/curl
LIB=0
INSTALL_BIN=y
INSTALL_LIB=n
BUILD_SCRIPT=(
    autoreconf -fi
    ./configure
    make
)
