CC=g++
SRC=https://github.com/KDE/yakuake
DEPENDENCIES=(cmake gcc make)
BUILDDIR=/tmp/pak/imports/yakuake
BIN=./build/bin/yakuake
LIB=0
INSTALL_BIN=y
INSTALL_LIB=n
BUILD_SCRIPT=(
    mkdir -p build
    cd build
    cmake ..
    make -j16
)
