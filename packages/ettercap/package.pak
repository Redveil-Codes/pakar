CC=gcc
SRC=https://github.com/Ettercap/ettercap
DEPENDENCIES=(cmake gcc make)
BUILDDIR=/tmp/pak/imports/ettercap
BIN=./build/src/ettercap
LIB=0
INSTALL_BIN=y
INSTALL_LIB=n
BUILD_SCRIPT=(
    mkdir -p build
    cd build
    cmake ..
    make -j16
)
