CC=gcc
SRC=https://github.com/netdata/netdata
DEPENDENCIES=(cmake gcc make)
BUILDDIR=/tmp/pak/imports/netdata
BIN=./build/netdata
LIB=0
INSTALL_BIN=y
INSTALL_LIB=n
BUILD_SCRIPT=(
    mkdir -p build
    cd build
    cmake ..
    make -j16
)
